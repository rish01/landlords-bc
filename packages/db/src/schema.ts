/**
 * Empty on purpose. PR-04 merges Better Auth CLI output (`user`, `session`,
 * `account`, `verification`, `two_factor`) plus `members` and RBAC tables.
 * Do not hand-roll `users.password_hash`.
 */
export {};

# Contract tests

Authorization isolation tests land in **PR-06** (`tests/contract/isolation.test.ts`).

CI must fail if a member can read another member’s private data. Do not skip these once they exist.
